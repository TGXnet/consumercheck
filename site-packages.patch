# site-packages
# diff -u old-file ny-file

# kiva/agg/agg.py
--- agg.orig.py 2015-05-02 11:41:54.880000000 +0200
+++ agg.py      2015-05-02 11:43:52.716000000 +0200
@@ -1115,7 +1115,7 @@
             rgb24 format.
         """
         FmtsWithoutAlpha = ('jpg', 'bmp', 'eps', "jpeg")
-        import Image as PilImage
+        from PIL import Image as PilImage
         size = (self.width(), self.height())
         fmt = self.format()

@@ -1140,7 +1140,7 @@
         else:
             bmp = self.bmp_array

-        img = PilImage.fromstring(pilformat, size, bmp.tostring())
+        img = PilImage.frombytes(pilformat, size, bmp.tostring())
         img.save(filename, format=file_format, options=pil_options)


# traitsui/qt4/tree_editor.py
--- tree_editor.orig.py 2014-01-01 23:37:42.000000000 +0100
+++ tree_editor.py      2015-05-06 08:10:57.027400000 +0200
@@ -1509,7 +1509,7 @@
         tree = self._tree
         for expanded, node, nid in self._object_info_for( object, name ):
             children = node.get_children( object )
-
+            expanded = True
             # Only add/remove the changes if the node has already been expanded:
             if expanded:
                 # Delete all current child nodes:
